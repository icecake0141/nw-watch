# Development dependencies for nw-watch
# Install with: python -m pip install -r requirements-dev.txt
#
# This file provides pinned versions for reproducible developer environments.
# The source-of-truth for dev dependencies is pyproject.toml [project.optional-dependencies.dev]
#
# To update these pins:
# 1. Install in a clean environment: pip install -e ".[dev]"
# 2. Get pinned versions: pip freeze | grep -E "(pytest|httpx|iniconfig|pluggy|httpcore|anyio|certifi|h11|idna)"
# 3. Manually update the version numbers below while preserving comments and structure

# Testing framework
pytest==9.0.2
pytest-asyncio==1.3.0
iniconfig==2.3.0
pluggy==1.6.0

# HTTP client for testing
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
certifi==2026.1.4
idna==3.6
anyio==4.12.1

# Additional linting tools (optional - not in pyproject.toml but recommended)
# Uncomment these if you want to use them in your development workflow:
# flake8==7.0.0
# black==24.2.0
# mypy==1.8.0
# pytest-cov==4.1.0
